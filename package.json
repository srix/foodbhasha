{
  "name": "foodbhasha",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node server.js",
    "test": "npx playwright test",
    "build:web": "rm -rf www && mkdir -p www && cp index.html privacy.html style.css app.js manifest.json robots.txt sitemap.xml www/ && cp -r data img www/",
    "sync": "npm run build:web && npx cap sync",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "android:build": "cd android && ./gradlew assembleDebug bundleRelease"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^8.0.0",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}